# Apps Script 웹앱 배포 URL (…/exec 로 끝나는 주소)
GAS_URL=https://script.google.com/macros/s/AKfycb.../exec

# Apps Script 편집기에서 setupToken() 을 1회 실행하면 로그에 찍히는 값
GAS_TOKEN=

# 관리자 페이지(/admin) 비밀번호
ADMIN_PASSWORD=

# 관리자 세션 쿠키 서명용 임의 문자열 (openssl rand -hex 32 등)
ADMIN_COOKIE_SECRET=
